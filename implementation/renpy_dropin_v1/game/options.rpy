## Minimal project options — placeholder art demo v0

define config.name = _("梦土回声 Demo v0")
define config.version = "0.1.0"

define config.has_sound = False
define config.has_music = False
define config.has_voice = False

## Avoid save compatibility issues during iteration
define config.save_directory = "mengtu_echo_demo_v0"
