# 项目设计与资料筛选

## 1. 研究问题

本项目评测 DeepSeek V4 在创造性 Agent Loop 中的产品质量、过程质量和系统可靠性，并根据稳定失败模式提出可能的常规后训练改进路径。项目不进行微调，也不声称验证某种后训练方法有效。

评测链路：任务与构念定义 → L0/L1/L2/L3 Loop 对照 → 轨迹记录 → 确定性检查 → 语义与人工评分 → 失败归因 → 后训练候选假设。

## 2. 公开研究与数据集筛选

### 采用

| 资料 | 用途 | 许可/边界 |
|---|---|---|
| DeepSeek 官方 API 文档 | 模型、思考模式、工具调用与运行记录依据 | 官方公开；运行前再次核对接口 |
| MacGyver | 约束创造问题解决与不可行错误类型 | Apache-2.0；首批优先新写平行题 |
| Reflexion | 反思、反馈和记忆 Loop 基线 | MIT；仅作 Agent 方法，不视为后训练 |
| CreativityPrism | 质量、创新性、多样性多维结构 | 采用构念思想，不复制未核许可数据 |
| Automated Creativity Evaluation | 发散性、任务满足、检索与 Judge 结合 | 采用方法思想，自行实现简化版 |

### 改造

| 资料 | 改造方式 |
|---|---|
| CDAT | 新写中文语境，先做适切性过滤再评新颖性 |
| DAT | 只作辅助语义距离指标，不作创造力总分 |
| AUT | 使用新物品与硬约束，加入适用性和安全性 |
| DRAT | 自建目标词、关系表与可验证连接 |
| AGC-Bench | 借鉴跨领域结构和 Judge 校准，不导入全部集合 |
| WritingBench | 借鉴实例相关 Rubric，必须经人工审核 |
| Creative Writing Benchmark | 借鉴多次生成、盲评、顺序反转和长度偏差控制 |

### 仅参考

LitBench、GEPA、TextGrad、DSPy/MIPROv2、AgentPRM、AutoTraj、DeepSeek-R1、DPO、InstructGPT、SemDis。它们用于 Judge 校准、Loop 迭代和后训练建议，不直接成为首批 Case 来源。

### 排除

TTCT 原题与商业量表；创造人格、自我效能、CAQ 等自陈量表直接给模型填写；BookMIA 原始长文本；科学研究构思作为首批主任务；实时互联网任务；无硬约束的纯自由写作；单一 LLM Judge 或单一语义距离评分。

## 3. 首批 36 个 Case

四个 Suite 各 9 例：

1. 心理学构念探针：3 个条件化发散、3 个约束 AUT、3 个发散—收敛连接。
2. 约束创造问题：手机支架、电缆整理、易碎品保护、滴水收集、狭长容器取物、快速冷却、状态门牌、文件防风、停电反射。
3. 创造产品：校园排队机制、剩余食物减量、微型入职、双受众规则、数值方向公告、不确定事件通知、无情绪词叙事、双重理解对话、状态一致分支故事。
4. 适应恢复：预算突降、关键材料不可用、工具冲突、Critic 错误否定、Critic 过奖新颖性、新增隐私约束、无关记忆、错误经验规则、达标停止。

所有 Case 当前均为 specification，正式题面、Gold 和评分锚点尚未冻结。

## 4. 评价结构

产品层：新颖性、适用性、多样性、灵活性、精致性、连贯性。  
过程层：探索宽度、早期固着、验证覆盖、批评质量、修订增益、恢复、停止、成本。  
可靠性层：重复运行、等价措辞、无关信息、工具顺序、约束突变、Judge 顺序反转和人工一致性。

不建立未经效度验证的单一创造力总分。

## 5. 后训练诊断

只有当失败跨 Loop 重复、工具环境正常、Case 和 Rubric 通过审计、人工确认且平行题复现时，才优先提出模型后训练候选。

常见映射：

- 候选同质 → 多样性条件 SFT/RFT、多目标偏好优化；
- 新颖但不可行 → 工具轨迹 SFT、DPO、PRM/轨迹奖励、RLVR；
- 约束遗漏 → 约束轨迹 SFT、遗漏偏好对、可验证奖励；
- 工具错误 → 函数调用 SFT、修复轨迹、Agent RL；
- 修订退化 → 草稿/修订偏好对、奖励模型、拒绝采样；
- 动态环境不重规划 → 多轮轨迹 SFT、课程学习、Agent RL；
- 记忆误用 → 记忆相关性数据、检索路由 SFT、偏好优化；
- Judge 投机 → 盲化人类偏好对、奖励模型重校准、多 Judge 蒸馏。

每条建议必须记录证据、训练数据构造、候选方法、预期指标、副作用和后续真实训练验证设计。

## 6. Rubric 审计

每条 Criterion 必须明确构念、检查对象、通过边界、失败边界、证据来源、严重度、自动 Oracle 和是否需人工复核。参考方案不能被误当作唯一合法答案。新颖性不能被生僻词或长度替代，流畅性只统计有效且非重复候选，灵活性按策略类别判断。
