name: Validate creative agent evaluation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-during-provenance-audit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation is temporarily paused while provenance and review-pack files are updated."
