name: Validate creative agent evaluation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-during-oracle-requirement-update:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation is temporarily paused while oracle requirement review files are updated."
